# Secrets — never commit these
.env
cookies.json
cookies.json.bak*

# Runtime state
state.json
state.json.bak*
state.json.migrated-*
state.db
state.db-wal
state.db-shm
state.db.corrupt-*
watcher.log

# Static HTML report (latest-only snapshot, regenerated each run)
report.html

# Generated by preview_email.py (local layout eyeballing)
email_preview.html

# Editor / agent dirs
.claude/
# Visual brainstorming companion mockups (superpowers) — local only
.superpowers/
# Superpowers design specs (brainstorming) — code + git history are the record
docs/superpowers/
# Local agent project context — may contain personal config; keep out of the repo
CLAUDE.md

# Local notes / planning — kept out of the repo
IDEAS.md
TODO.txt
research/

# Python / uv
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.python-version

# Local /sell_item capture (account-specific marketplace data), kept out of the
# repo. tests/test_shop_api.py builds a synthetic equivalent when it's missing.
tests/fixtures_sell_item.json
